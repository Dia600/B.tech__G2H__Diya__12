
#include <stdio.h>

int main() {
    int size,i ;
    printf("Enter size of an array:");
    scanf("%d",&size);
    int arr1[size];
    printf("Enter %d elements of an array:\n",size);
    for (i=0; i<size; i++){
        scanf("%d",&arr1[i]);}
        int arr2[size];
        printf("array 2 is :");
    for (i=0;i<size;i++){
        arr2[i] = arr1[i];
        printf("%d ",arr2[i]);
    }
        
    
    
    
    return 0;
}

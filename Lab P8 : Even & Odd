#include <stdio.h>
#include <math.h>
int main() {
    int num;
    printf("Enter num =");
    scanf("%d",&num);
    if (num%2 != 0)
    printf("Number is odd");
    else 
    printf("Number is even");
    return 0;
}

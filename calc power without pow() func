#include <stdio.h>


int main() {

   int n,i,y,prod=1;
   
   printf("Enter base: ");
   scanf("%d", &n);
   printf("Enter power: ");
   scanf("%d",&y);
   for (i=1;i<=y;i++)
   {
       prod *= n;
   }
   printf("%d^%d is:%d",n,y,prod);
   
   return 0;
}
